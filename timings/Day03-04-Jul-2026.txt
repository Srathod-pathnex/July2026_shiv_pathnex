9-11= youtube lecture
11:30-2= practice of linux
4:30-5:30= Q&A session
6-9:30= 	1.launched apache server using ansible
		2.docker image using `docker build -t pathnex-nginx .`.
		3. Started the Nginx container with `docker run -d -p 8000:80 --name nginx-container pathnex-nginx:latest`.
		4. Verified it was running using `docker ps` and accessed the application at `http://localhost:8000`
		5.problem solving.
